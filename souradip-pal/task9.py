# Import the numpy library and give it the alias 'np'
import numpy as np

# 9. Create a numpy array of strings

# Create a numpy array 'a' that contains three strings: 'Toronto', 'Montreal', and 'New York'.
a = np.array(('Toronto', 'Montreal', 'New York'))

# Print the content of array 'a', which contains the strings.
print(a)

# Print the data type of the elements in array 'a'.
print(a.dtype)
