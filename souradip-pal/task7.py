# Import the numpy library and give it the alias 'np'
import numpy as np

# 7. Create a numpy array with random integers

# Create a numpy array 'a' that contains 5 random integers between 0 and 9 (inclusive).
a = np.random.randint(10, size=5)

# Print the content of array 'a', which contains the random integers.
print(a)
